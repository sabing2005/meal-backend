<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Verify your email</title>
    <style>
      body { font-family: Arial, sans-serif; background:#f6f9fc; padding:24px; }
      .card { max-width:560px; margin:0 auto; background:#ffffff; border:1px solid #e6e6e6; border-radius:8px; padding:24px; }
      .title { font-size:20px; font-weight:600; margin:0 0 12px; }
      .text { color:#333333; line-height:1.5; margin:0 0 16px; }
      .btn { display:inline-block; background:#16a34a; color:#ffffff !important; text-decoration:none; padding:10px 16px; border-radius:6px; }
      .footer { color:#9ca3af; font-size:12px; margin-top:24px; border-top:1px solid #e5e7eb; padding-top:12px; }
    </style>
  </head>
  <body>
    <div class="card">
      <p class="title"><%= appName || 'Your App' %> - Verify your email</p>
      <p class="text">Hello <%= userName || 'there' %>,</p>
      <p class="text">Please confirm your email address to complete your registration.</p>
      <p>
        <a class="btn" href="<%= verifyUrl %>" target="_blank" rel="noopener noreferrer">Verify Email</a>
      </p>
      <p class="text">If the button does not work, copy and paste this URL into your browser:<br/>
        <a href="<%= verifyUrl %>" target="_blank" rel="noopener noreferrer"><%= verifyUrl %></a>
      </p>
      <div class="footer">© <%= new Date().getFullYear() %> <%= appName || 'Your App' %>. All rights reserved.</div>
    </div>
  </body>
</html> 